## Learned User Preferences

- When changing this Raycast GitLab extension, preserve existing code style and structure; do not refactor unrelated patterns or drift from local conventions.
- Run requested terminal, lint, and Raycast dev/debug commands yourself when asked instead of only describing them.
- Communicate in Russian in chat; code comments, UI copy, and docs follow existing project language.
- Never commit or push without explicit user request.
- Refactoring must preserve current behavior unless the user explicitly requests a behavior change.
- API/GraphQL types and selections: include only fields used in UI and actions; do not expand response schemas with unused properties during refactors.
- Data fetching: prefer `useCachedPromise`, `useCachedState`, and `Cache` from `@raycast/utils` over custom cache hooks; prefer `usePromise` over `useState`+`useEffect` when async fetch is the only state update path; handle errors inline or via hook `onError` (not toast-only `useEffect`). When the call site shows errors, pass `onError: () => undefined` to suppress default utility toasts.
- React style: declare JSX inline—do not store elements in `useMemo`; PascalCase for functions that return components; inline 1–3 line single-use expressions (except enum/mapping tables); omit redundant guards when types/API contracts already guarantee values.
- Naming: prefer short or full names for parameters and variables over one-letter names (including closure args)—e.g. `branch`/`repo` or `currentBranch`/`selectedRepository`, not `s`/`m`/`b` or opaque abbreviations like `brch`/`rp`; don't over-lengthen names when local context makes a shorter name sufficient.
- Dates: `formatDate` for relative display and `formatDateTime` for absolute strings (tooltips, etc.) in `utils.ts`; avoid ad-hoc `toLocaleString` or legacy date helpers.

## Learned Workspace Facts

- Search Merge Requests: UI in `src/components/mr_search.tsx` (entry `src/mr_search.tsx`); AI tool `src/tools/search-merge-requests.ts` is separate. `MyProjectsDropdown` with `includeAllItem={false}` (project required before load); sections "Created by me", "Assigned to Review" (hidden when empty), "Other"; Sort By (`mr_sort.tsx`, ⌘S) sets GraphQL `MergeRequestSort` ("Default" omits sort). Search MR prefs use `useCachedState`; fetch uses `usePaginatedMergeRequests` → GraphQL in `mr_gql.ts`.
- `MyProjectsDropdown` in `src/components/project.tsx` supports `includeAllItem` (default `true`); My Merge Requests and Reviews keep "All Projects". My MR fetch (`useMyMergeRequests` in `mr_my.tsx`) uses `usePaginatedMergeRequests` (GraphQL `currentUser.*MergeRequests`); cache key excludes project—filter stays client-side in `MyMergeRequests`.
- My Merge Requests hides author subtitle and avatar on list rows via `showAuthor={false}` on `MRListItem`; Search MR and other MR lists keep the default (`showAuthor` true). MR list row subtitle is hidden while the side panel is open.
- MR list side panel: `useMRListDetails()` + `MRListDetailsToggleAction` (⌘⇧D). Metadata column: `useMRListMetadata()` + `MRListMetadataToggleAction` (⌘⇧I, only when side panel open). Side panel uses `MRListDetailMetadata`, full detail uses `MRDetailMetadata` in `mr_metadata.tsx`; the former `listdetails` preference was removed from `package.json`.
- Shared MR UI in `mr.tsx`, `mr_actions.tsx`, `mr_metadata.tsx`, `mr_discussions.ts`, `mr_status.ts`. MR list fetch (`mr_gql.ts` + `mr_data.ts`) includes `headPipeline`, `resolvedDiscussionsCount`, and `resolvableDiscussionsCount` in one GraphQL query—no per-row pipeline or discussion fetches on list rows. `useMRDiscussionStats` uses list counts when present; REST discussions fetch remains fallback only when counts are absent. Metadata label is "Discussions" (`resolved/resolvable` only; not `user_notes_count`); no discussion count in list accessories. MR list Copy section (`MRCopySection` in `mr_actions.tsx`): Copy URL (⌘.), Copy Markdown (⌘⌃.) on list rows.
- My Merge Requests and Reviews use GraphQL default sort (`CREATED_DESC`); no client-side re-sort.
- MR list GraphQL (`mr_gql.ts`): project/group use `mergeRequests`; global My MR uses `currentUser.authoredMergeRequests` / `assignedMergeRequests` / `reviewRequestedMergeRequests` by scope; cursor pagination (page size 20) via `fetchMergeRequestsGqlPage`; selections include only UI/actions-used fields from `schema.graphql`. GraphQL `avatarUrl` may be relative on self-hosted—resolve via `gitlab.joinUrl()` when mapping users. MR side panel/detail reuses list `MergeRequest` for metadata; `useDetail` fetches description via `fetchMergeRequestGqlByGlobalId`. Single MR by project uses `fetchMergeRequestGqlByProjectIid`. AI tool `search-merge-requests.ts` uses `fetchMergeRequestsGqlList`. REST MR list/get endpoints removed from `gitlabapi.ts`.
- `MRListItem` must not call GitLab per row for approvals, full MR, discussions, or pipelines. Pipeline accessory uses `head_pipeline` from list payload (`getMRHeadPipelineStatus`); `getCIJobStatusIcon`; hidden when `has_conflicts`. Full MR pipelines use `useMRPipelines(mr, { all: true })` in `MRPipelineList`. Full MR/approvals fetches belong in side panel (`MRListDetail`) or detail (`MRDetail`) only.
- Pipeline list (`pipelines.tsx`): subtitle short SHA; date accessory `formatPipelineListAccessoryDate` in `utils.ts`. Pipeline row actions (`pipeline_actions.tsx`): Retry (confirmAlert, ⌘R), Cancel (destructive), Run Pipeline (confirmAlert, ⌘N), Copy URL (⌘.) in first section; Refresh without ⌘R. Jobs list (`jobs.tsx`): no accessories; status on icon tooltip; `manual` → `Icon.Gear` in `getCIJobStatusIcon`. Job actions (`job_actions.tsx`): Retry/Cancel/Run, Copy URL (⌘.), Download Artifact submenu (⌘D, only when `artifacts` non-empty); Refresh without ⌘R. Artifact download: preference `artifactDownloadDirectory` (default `~/Downloads`); `trace` → `/jobs/:id/trace`, `archive` → `/jobs/:id/artifacts`; open with `open()` not `showInFinder`. Prefer REST jobs fetch (`pipelineID`) for `artifacts` field. Shared copy shortcuts in `utils.ts`: `copyShortcut` (⌘.), `copyMarkdownShortcut` (⌘⌃.), `copySecondaryShortcut` (⌘⇧.).
- Commits list (`commits/`): no per-row `useCommitStatus` in `CommitListItem` or `EventCommitListItem`; static commit icon (`Color.SecondaryText`). `useCommitStatus` (`commits/utils.ts`) only for `branch.tsx`. `CommitListItem` uses `useUserAvatar` (`hooks/useUserAvatar.ts`, GitLab `/avatar` by email, Raycast `Cache`, in-flight dedup) with commit icon fallback when `author_email` missing. `MRCommitList` / `ProjectCommitList` paginate via `useCachedPromise` in `commits/data.ts`.
- Author subtitle/tooltip email uses `User.public_email` when the API provides it. Import the `gitlab` client from `src/common.ts`, not `src/gitlabapi.ts`.
- Caching: component data hooks use `useCachedPromise`/`usePromise`; the `useCache` React hook was removed from `cache.ts`. File-cache helpers (`receiveLargeCachedObject`, `getLargeCacheDirectory`, image dir via `icons.ts`) remain for large/static payloads in `gitlabapi.ts` and avatars. "Clear Local Extension Cache" action removed (`cache_actions.tsx`).
